<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elegant Profile Card</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link rel="preconnect" href__="https://fonts.googleapis.com">
    <link rel="preconnect" href__="https://fonts.gstatic.com" crossorigin>
    <link href__="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        .modal-backdrop {
            background-color: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(5px);
        }
        .modal {
            transition: opacity 0.3s ease, transform 0.3s ease;
        }
        .modal.hidden {
            opacity: 0;
            transform: scale(0.95);
            pointer-events: none;
        }
    </style>
</head>
<body class="bg-gray-50">

    <div class="min-h-screen bg-gradient-to-br from-gray-50 via-white to-gray-50">
        <div class="container mx-auto px-4 py-12 max-w-6xl">
            <!-- Header -->
            <div class="text-center mb-12">
                <h1 class="text-4xl font-light text-gray-900 mb-4 tracking-tight">
                    Profile Settings
                </h1>
                <p class="text-lg text-gray-500 font-light">
                    Manage your account and preferences
                </p>
            </div>

            <div class="grid lg:grid-cols-3 gap-8">
                <!-- Main Profile Card -->
                <div class="lg:col-span-2">
                    <div class="border-0 shadow-xl shadow-gray-200/30 bg-white overflow-hidden rounded-2xl">
                        <!-- Header Gradient -->
                        <div class="h-32 bg-gradient-to-r from-blue-600 via-purple-600 to-blue-800 relative">
                            <div class="absolute inset-0 bg-black/10"></div>
                            <!-- Edit Button -->
                            <button id="openEditModalBtnMain" class="absolute top-4 right-4 text-white/80 hover:text-white hover:bg-white/20 transition-all duration-200 p-2 rounded-full">
                                <i data-lucide="edit-3" class="w-4 h-4"></i>
                            </button>
                        </div>

                        <div class="p-8 -mt-16 relative z-10">
                            <!-- Avatar Section -->
                            <div class="text-center mb-8">
                                <div class="relative inline-block group">
                                    <img id="profileAvatar" src="https://i.pravatar.cc/150?u=a042581f4e29026704d" alt="Profile Avatar" class="w-32 h-32 rounded-full mx-auto border-4 border-white shadow-xl object-cover">
                                </div>
                                <!-- Name and Role -->
                                <div class="mt-6">
                                    <h2 id="profileFullName" class="text-3xl font-light text-gray-900 mb-2">Alex Doe</h2>
                                    <span id="profileRole" class="inline-flex items-center gap-2 bg-gradient-to-r from-purple-500 to-pink-500 text-white px-4 py-1 text-sm font-medium shadow-md rounded-full">
                                        <i data-lucide="crown" class="w-4 h-4"></i>
                                        Admin
                                    </span>
                                </div>
                                <!-- Bio -->
                                <p id="profileBio" class="text-gray-600 mt-4 max-w-md mx-auto leading-relaxed">
                                    Digital craftsman building beautiful interfaces & experiences. Based in the cloud.
                                </p>
                            </div>

                            <!-- Contact Info -->
                            <div class="grid md:grid-cols-2 gap-6 mb-8">
                                <div class="flex items-center gap-3 p-4 bg-gray-50 rounded-xl">
                                    <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="mail" class="w-5 h-5 text-blue-600"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm text-gray-500">Email</p>
                                        <p id="profileEmail" class="font-medium text-gray-900">alex.doe@example.com</p>
                                    </div>
                                </div>
                                <div class="flex items-center gap-3 p-4 bg-gray-50 rounded-xl">
                                    <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="calendar" class="w-5 h-5 text-green-600"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm text-gray-500">Member since</p>
                                        <p id="profileMemberSince" class="font-medium text-gray-900">May 2023</p>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Action Buttons -->
                            <div class="grid md:grid-cols-2 gap-4">
                                <button id="openPasswordModalBtn" class="w-full border-gray-200 hover:border-gray-300 transition-all duration-200 py-4 px-4 rounded-lg border flex items-center justify-center gap-2">
                                    <i data-lucide="shield" class="w-4 h-4"></i> Change Password
                                </button>
                                <button id="logoutBtn" class="w-full border-red-200 text-red-600 hover:border-red-300 hover:bg-red-50 transition-all duration-200 py-4 px-4 rounded-lg border flex items-center justify-center gap-2">
                                    <i data-lucide="log-out" class="w-4 h-4"></i> Sign Out
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Sidebar -->
                <div class="space-y-6">
                    <div class="border-0 shadow-lg shadow-gray-100/50 bg-white/80 backdrop-blur-sm rounded-2xl p-8">
                        <h3 class="text-lg font-medium text-gray-900 mb-6 flex items-center gap-2">
                            <i data-lucide="user" class="w-5 h-5 text-gray-600"></i> Account Overview
                        </h3>
                        <div class="space-y-4">
                            <button id="openEditModalBtnSidebar" class="w-full justify-start text-left hover:bg-gray-50 transition-all duration-200 p-3 rounded-lg flex items-center gap-3">
                                <i data-lucide="edit-3" class="w-4 h-4 text-blue-600"></i> Edit Profile
                            </button>
                            <button id="openPasswordModalBtnSidebar" class="w-full justify-start text-left hover:bg-gray-50 transition-all duration-200 p-3 rounded-lg flex items-center gap-3">
                                <i data-lucide="shield" class="w-4 h-4 text-green-600"></i> Change Password
                            </button>
                            <button id="logoutBtnSidebar" class="w-full justify-start text-left hover:bg-red-50 text-red-600 transition-all duration-200 p-3 rounded-lg flex items-center gap-3">
                                <i data-lucide="log-out" class="w-4 h-4"></i> Sign Out
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Profile Modal -->
    <div id="editProfileModal" class="modal hidden fixed inset-0 z-50 flex items-center justify-center">
        <div class="modal-backdrop fixed inset-0"></div>
        <div class="bg-white rounded-lg shadow-2xl p-8 m-4 max-w-2xl w-full relative max-h-[90vh] overflow-y-auto">
            <div class="text-center pb-6">
                <h2 class="text-2xl font-light">Edit Profile</h2>
                <p class="text-gray-500">Update your personal information</p>
            </div>
            <div class="space-y-8">
                <!-- Avatar Section -->
                <div class="text-center">
                    <div class="relative inline-block">
                        <img id="editAvatarPreview" src="https://i.pravatar.cc/150?u=a042581f4e29026704d" alt="Avatar Preview" class="w-24 h-24 rounded-full mx-auto border-4 border-gray-100 object-cover">
                    </div>
                    <div class="mt-4">
                        <input type="file" accept="image/*" id="avatarUploadInput" class="hidden">
                        <label for="avatarUploadInput" class="cursor-pointer inline-flex items-center gap-2 px-4 py-2 border border-gray-300 rounded-lg text-sm hover:bg-gray-50">
                            <i data-lucide="camera" class="w-4 h-4"></i> Change Avatar
                        </label>
                    </div>
                </div>
                <!-- Form Fields -->
                <div class="grid md:grid-cols-2 gap-6">
                    <div>
                        <label for="editFullName" class="text-sm font-medium text-gray-700 block mb-2">Full Name</label>
                        <input id="editFullName" type="text" placeholder="Your full name" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    </div>
                    <div>
                        <label for="editRole" class="text-sm font-medium text-gray-700 block mb-2">Role</label>
                        <input id="editRole" type="text" placeholder="e.g., Admin, User" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    </div>
                </div>
                <div>
                    <label for="editBio" class="text-sm font-medium text-gray-700 block mb-2">Bio</label>
                    <textarea id="editBio" placeholder="Tell us about yourself..." class="w-full px-3 py-2 border border-gray-300 rounded-lg h-24 resize-none focus:ring-blue-500 focus:border-blue-500"></textarea>
                </div>
                 <!-- Action Buttons -->
                <div class="flex gap-3 pt-6">
                    <button id="closeEditModalBtn" class="flex-1 px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-100 flex items-center justify-center gap-2">
                         <i data-lucide="x" class="w-4 h-4"></i> Cancel
                    </button>
                    <button id="saveProfileBtn" class="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center justify-center gap-2">
                        <i data-lucide="save" class="w-4 h-4"></i> Save Changes
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Change Password Modal -->
    <div id="changePasswordModal" class="modal hidden fixed inset-0 z-50 flex items-center justify-center">
        <div class="modal-backdrop fixed inset-0"></div>
        <div class="bg-white rounded-lg shadow-2xl p-8 m-4 max-w-md w-full relative">
             <div class="text-center pb-6">
                <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <i data-lucide="shield" class="w-6 h-6 text-blue-600"></i>
                </div>
                <h2 class="text-xl font-semibold">Change Password</h2>
                <p class="text-gray-500">Ensure your account stays secure</p>
            </div>
            <div class="space-y-6">
                <div class="p-3 border-amber-200 bg-amber-50 rounded-lg text-amber-800 text-sm">
                    <strong>Note:</strong> This is a stubbed modal for demonstration.
                </div>
                <div>
                    <label for="currentPassword" class="text-sm font-medium text-gray-700 block mb-2">Current Password</label>
                    <input id="currentPassword" type="password" placeholder="Enter your current password" class="w-full px-3 py-2 border border-gray-300 rounded-lg">
                </div>
                 <div>
                    <label for="newPassword" class="text-sm font-medium text-gray-700 block mb-2">New Password</label>
                    <input id="newPassword" type="password" placeholder="Enter new password" class="w-full px-3 py-2 border border-gray-300 rounded-lg">
                </div>
                 <div>
                    <label for="confirmPassword" class="text-sm font-medium text-gray-700 block mb-2">Confirm New Password</label>
                    <input id="confirmPassword" type="password" placeholder="Confirm new password" class="w-full px-3 py-2 border border-gray-300 rounded-lg">
                </div>
                 <div class="flex gap-3 pt-4">
                    <button id="closePasswordModalBtn" class="flex-1 px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-100 flex items-center justify-center gap-2">
                        <i data-lucide="x" class="w-4 h-4"></i> Cancel
                    </button>
                    <button class="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center justify-center gap-2">
                        <i data-lucide="shield" class="w-4 h-4"></i> Change Password
                    </button>
                </div>
            </div>
        </div>
    </div>


    <script>
        document.addEventListener('DOMContentLoaded', () => {
            lucide.createIcons();

            // --- DOM Elements ---
            const profileElements = {
                avatar: document.getElementById('profileAvatar'),
                fullName: document.getElementById('profileFullName'),
                role: document.getElementById('profileRole'),
                bio: document.getElementById('profileBio'),
                email: document.getElementById('profileEmail'),
                memberSince: document.getElementById('profileMemberSince')
            };

            const editModal = document.getElementById('editProfileModal');
            const passwordModal = document.getElementById('changePasswordModal');

            const editForm = {
                avatarPreview: document.getElementById('editAvatarPreview'),
                avatarUploadInput: document.getElementById('avatarUploadInput'),
                fullName: document.getElementById('editFullName'),
                role: document.getElementById('editRole'),
                bio: document.getElementById('editBio')
            };
            
            // --- State ---
            let currentUser = {
                avatar_url: 'https://i.pravatar.cc/150?u=a042581f4e29026704d',
                full_name: 'Alex Doe',
                role: 'Admin',
                bio: 'Digital craftsman building beautiful interfaces & experiences. Based in the cloud.',
                email: 'alex.doe@example.com',
                member_since: 'May 2023'
            };

            // --- Functions ---
            const renderProfile = (user) => {
                profileElements.avatar.src = user.avatar_url;
                profileElements.fullName.textContent = user.full_name;
                profileElements.role.innerHTML = `<i data-lucide="crown" class="w-4 h-4"></i> ${user.role}`;
                profileElements.bio.textContent = user.bio;
                profileElements.email.textContent = user.email;
                profileElements.memberSince.textContent = user.member_since;
                lucide.createIcons();
            };

            const openEditModal = () => {
                editForm.avatarPreview.src = currentUser.avatar_url;
                editForm.fullName.value = currentUser.full_name;
                editForm.role.value = currentUser.role;
                editForm.bio.value = currentUser.bio;
                editModal.classList.remove('hidden');
            };

            const closeEditModal = () => editModal.classList.add('hidden');
            const openPasswordModal = () => passwordModal.classList.remove('hidden');
            const closePasswordModal = () => passwordModal.classList.add('hidden');

            const saveProfile = () => {
                const newProfile = {
                    ...currentUser,
                    avatar_url: editForm.avatarPreview.src,
                    full_name: editForm.fullName.value,
                    role: editForm.role.value,
                    bio: editForm.bio.value,
                };
                
                // TODO: Replace localStorage with your Firebase Firestore call
                localStorage.setItem('userProfile', JSON.stringify(newProfile));
                alert('Profile saved to localStorage!');
                
                currentUser = newProfile;
                renderProfile(currentUser);
                closeEditModal();
            };

            const loadProfile = () => {
                // TODO: Replace localStorage with your Firebase call to get user data
                const savedProfile = localStorage.getItem('userProfile');
                if (savedProfile) {
                    currentUser = JSON.parse(savedProfile);
                }
                renderProfile(currentUser);
            };

            // --- Event Listeners ---
            document.getElementById('openEditModalBtnMain').addEventListener('click', openEditModal);
            document.getElementById('openEditModalBtnSidebar').addEventListener('click', openEditModal);
            document.getElementById('closeEditModalBtn').addEventListener('click', closeEditModal);

            document.getElementById('openPasswordModalBtn').addEventListener('click', openPasswordModal);
            document.getElementById('openPasswordModalBtnSidebar').addEventListener('click', openPasswordModal);
            document.getElementById('closePasswordModalBtn').addEventListener('click', closePasswordModal);
            
            document.getElementById('saveProfileBtn').addEventListener('click', saveProfile);
            
            // Logout Stubs
            document.getElementById('logoutBtn').addEventListener('click', () => alert('Logout clicked! (Stub)'));
            document.getElementById('logoutBtnSidebar').addEventListener('click', () => alert('Logout clicked! (Stub)'));

            // Avatar Upload Preview
            editForm.avatarUploadInput.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        // This Data URL can be uploaded to Firebase Storage
                        editForm.avatarPreview.src = e.target.result; 
                    };
                    reader.readAsDataURL(file);
                }
            });

            // --- Initial Load ---
            loadProfile();
        });
    </script>
</body>
</html>
